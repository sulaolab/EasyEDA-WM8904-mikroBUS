# Fleet EOL policy: normalize text to LF in the repo, check out CRLF on Windows.
* text=auto eol=crlf

# Binary assets (never EOL-converted). .eprj is a SQLite database -- EOL
# conversion would corrupt it.
*.eprj binary
*.png  binary
*.pdf  binary
